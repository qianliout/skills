interface:
  display_name: "Go"
  short_description: "Go code entrypoint and layer routing rules"
  default_prompt: "Use $go as the only Go entrypoint, then read only the layer or feature references required by the task."
