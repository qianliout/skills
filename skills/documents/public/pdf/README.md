# pdf

## 简介

读取、创建、渲染和检查 PDF，兼顾内容与页面布局。

## 来源

OpenAI Codex PDF 插件，运行时包版本 `26.619.11828`。

## 安装方案

原始版本随 Codex PDF 插件安装；使用时还需要插件声明的 PDF 工具链。

本仓库统一安装：在仓库根目录运行 `./scripts/install.sh`，脚本完成公共源码更新后，将该 Skill 复制到 `~/.agents/skills/pdf`。

