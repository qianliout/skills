# 灌 schema 的文件顺序，一行一个，相对 --schema-dir。
# 以 # 开头的行是注释。列表里不存在的文件会打印「跳过」而不是报错——
# 这是刻意的：源码仓里常有跳号（如 m060/m061 不存在）。
# 顺序错了比漏了更糟：外键/索引会建不上。基线在前，增量按编号在后。

mldong-plus1.0.pgsql.sql
m02_mini_user.sql
m010_mini_permission.sql
m011_fix_mini_permissions.sql
m007.sql
m008.sql
