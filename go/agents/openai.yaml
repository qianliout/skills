interface:
  display_name: "Go"
  short_description: "Go code entrypoint and layer routing rules"
  default_prompt: "Use $go when writing, refactoring, or reviewing Go code, then apply specific Go layer skills only when their layer or feature is involved."
