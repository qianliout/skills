# Jenkins MCP endpoint. The Jenkins plugin recommends Streamable HTTP:
#   https://your-jenkins.example.com/mcp-server/mcp
JENKINS_MCP_URL="https://your-jenkins.example.com/mcp-server/mcp"

# Generate with:
#   echo -n "jenkins_username:jenkins_api_token" | base64
JENKINS_MCP_BASIC_AUTH_B64=""
